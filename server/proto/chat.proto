syntax = "proto3";

package chat;


service ChatService{
    rpc Chat(chatRequest) returns (stream chatChunk); 
}

message chatRequest{
    string userId = 1;
    string message = 2;
}

message chatChunk{
    string content = 1;
    string thinking = 2;
    bool done = 3;
    string error = 4;
}
